{
  "dependencies": {
    "create-react-app": "^5.0.1",
    "express": "^4.18.1",
    "node": "^18.5.0"
  },
  "scripts": {
    "start": "cd astro_server && npm install && node server.js",
    "heroku-postbuild": "cd astro_client && npm install && npm run build"
  }
}
